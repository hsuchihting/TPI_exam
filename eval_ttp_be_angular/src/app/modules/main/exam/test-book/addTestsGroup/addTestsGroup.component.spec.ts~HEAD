import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AddTestsGroupComponent } from './addTestsGroup.component';

describe('AddTestsGroupComponent', () => {
  let component: AddTestsGroupComponent;
  let fixture: ComponentFixture<AddTestsGroupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AddTestsGroupComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AddTestsGroupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
